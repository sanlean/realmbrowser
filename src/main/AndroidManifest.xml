<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="io.realm.browser"
    android:versionCode="163"
    android:versionName="0.1.15" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="24" />

    <application
        android:allowBackup="true"
        android:supportsRtl="true" >
        <activity
            android:name="io.realm.browser.BrowserActivity"
            android:theme="@style/realm_browser_RealmBrowserTheme" />

        <service
            android:name="io.realm.browser.RealmBrowserService"
            android:exported="false" />
    </application>

</manifest>